
/**
 * File generated by Atek tsgen
 * env=host
 * DO NOT MODIFY
 */
import { URL } from 'url';
import { ApiBrokerClient } from '@atek-cloud/api-broker';

export const ID = "atek.cloud/system-api";
export const REVISION = undefined;

export default class SystemApiClient extends ApiBrokerClient {
  constructor() {
    super("atek.cloud/system-api")
  }

  getBucket(bucketId: string): Promise<Bucket> {
    return this.$rpc("getBucket", [bucketId])
  }
}

export interface Bucket {
  id: string;
  type: BucketTypeEnum;
  title: string;
  items: BucketChild[];
}

export interface BucketChild {
  id: string;
  type: BucketTypeEnum;
  title: string;
}

export enum BucketTypeEnum {
  root = 'bucket:root',
  app = 'bucket:app',
  trash = 'bucket:trash',
  db = 'db'
}
